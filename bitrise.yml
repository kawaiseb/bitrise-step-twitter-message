format_version: 4
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

app:
  envs:
  - BITRISE_STEP_ID: twitter-message
  - BITRISE_STEP_VERSION: "0.0.1"
  - BITRISE_STEP_GIT_CLONE_URL: https://github.com/kawaiseb/bitrise-step-twitter-message
  - MY_STEPLIB_REPO_FORK_GIT_URL: https://github.com/kawaiseb/bitrise-steplib

  - TW_DEBUG_MODE: "yes"
  - TW_CONSUMER_KEY: "ctDQvfwI7ppvfGi6o8qLsc9ZN"
  - TW_CONSUMER_SECRET: "vW25CIp9OMbqByNbtKtxkdPGFShUa4ByMGVXIiVPcEdfaJiUg8"
  - TW_ACCESS_TOKEN_KEY: "zMwq2MmLckeQ51cGGpG6RYmeVWdcCn7edTi2wHJ1ALtVc"
  - TW_ACCESS_TOKEN_SECRET: "2783726838-djGDWFEaIKKBmVjVpVDQmvd4XfqJ1NNyIGVstXY"
  - TW_SCREEN_NAME: "kawaiseb"
  - TW_STATUS: "This message will appear on the screen_name account"

workflows:
  test:
    before_run:
    - audit-this-step
    steps:
    - change-workdir:
        title: Switch working dir to test / _tmp dir
        run_if: true
        inputs:
        - path: "$BITRISE_SOURCE_DIR/_tmp"
        - is_create_path: true
    
    - script:
        inputs:
        - content: |
            #!/bin/bash
            echo "Step twitter-message..."
            echo "TW_DEBUG_MODE: $TW_DEBUG_MODE"

    - path::./:
        title: Step Test
        inputs:
        - tw_debug_mode: $TW_DEBUG_MODE
        - tw_consumer_key: $TW_CONSUMER_KEY
        - tw_consumer_secret: $TW_CONSUMER_SECRET
        - tw_access_token_key: $TW_ACCESS_TOKEN_KEY
        - tw_access_token_secret: $TW_ACCESS_TOKEN_SECRET
        - tw_screen_name: $TW_SCREEN_NAME
        - tw_status: $TW_STATUS 
    - script:
        inputs:
        - content: |
            #!/bin/bash
            echo "This output was generated by the Step (EXAMPLE_STEP_OUTPUT): $EXAMPLE_STEP_OUTPUT"


  # ----------------------------------------------------------------
  # --- workflows to Share this step into a Step Library
  audit-this-step:
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            stepman audit --step-yml ./step.yml

  share-this-step:
    envs:
      # if you want to share this step into a StepLib
      - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL
      - BITRISE_STEP_ID: $BITRISE_STEP_ID
      - BITRISE_STEP_VERSION: $BITRISE_STEP_VERSION
      - BITRISE_STEP_GIT_CLONE_URL: $BITRISE_STEP_GIT_CLONE_URL
    description: |-
      If this is the first time you try to share a Step you should
      first call: $ bitrise share

      This will print you a guide, and information about how Step sharing
      works. Please read it at least once!

      As noted in the Step sharing guide you'll have to fork the
      StepLib you want to share this step into. Once you're done with forking
      the repository you should set your own fork's git clone URL
      in the `.bitrise.secrets.yml` file, or here in the `envs` section,
      as the value of the `MY_STEPLIB_REPO_FORK_GIT_URL` environment.

      You're now ready to share this Step, just make sure that
      the `BITRISE_STEP_ID` and `BITRISE_STEP_VERSION`
      environments are set to the desired values!

      To share this Step into a StepLib you can just run: $ bitrise run share-this-step

      Once it finishes the only thing left is to actually create a Pull Request,
      the way described in the guide printed at the end of the process.
    before_run:
    - audit-this-step
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            bitrise share start -c "${MY_STEPLIB_REPO_FORK_GIT_URL}"
            bitrise share create --stepid "${BITRISE_STEP_ID}" --tag "${BITRISE_STEP_VERSION}" --git "${BITRISE_STEP_GIT_CLONE_URL}"
            bitrise share finish
